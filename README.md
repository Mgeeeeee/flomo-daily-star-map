# DailyMap

这个仓库只用于备份一个 Codex skill 包。

## GitHub 结构

- `README.md`
- `flomo-daily-star-map/`

其中 `flomo-daily-star-map/` 保持和原始压缩包一致，只包含：

- `SKILL.md`
- `templates/viewer.html`

本地调试、测试数据、生成产物、脚本和压缩包都不进入这个仓库结构。

